<!DOCTYPE html><html lang="id"><head>
<meta charset="UTF-8"/><meta name="viewport" content="width=device-width,initial-scale=1.0"/>
<title>Bukti Pembayaran <%= p.receipt_no %></title>
<link href="/public/css/output.css" rel="stylesheet"/>
<style>
@media print {
  .no-print { display: none; }
  body { background: #fff; }
}
</style>
</head><body class="bg-gray-50">
<div class="max-w-3xl mx-auto bg-white p-6 my-6 shadow">
  <div class="flex justify-between items-start">
    <div>
      <h1 class="text-xl font-semibold">Taman Pengajian Al-Qur'an</h1>
      <div class="text-sm text-gray-600">Bukti Pembayaran</div>
    </div>
    <div class="text-right">
      <div class="text-sm">No. Bukti</div>
      <div class="font-semibold"><%= p.receipt_no %></div>
    </div>
  </div>

  <hr class="my-4"/>

  <div class="grid grid-cols-2 gap-4 text-sm">
    <div>
      <div><span class="text-gray-500">Santri:</span> <%= p.santri_name %></div>
      <div><span class="text-gray-500">NIS:</span> <%= p.nis %></div>
    </div>
    <div class="text-right">
      <div><span class="text-gray-500">Tanggal:</span> <%= new Date(p.payment_date).toLocaleString('id-ID') %></div>
      <div><span class="text-gray-500">Metode:</span> <%= p.method %></div>
    </div>
  </div>

  <table class="min-w-full text-sm mt-4">
    <thead class="bg-gray-100">
      <tr><th class="text-left p-2">Periode</th><th class="text-left p-2">Nama Tagihan</th><th class="text-right p-2">Nominal</th></tr>
    </thead>
    <tbody>
      <% details.forEach(d => { %>
        <tr class="border-t">
          <td class="p-2"><%= d.period_year %>-<%= String(d.period_month).padStart(2,'0') %></td>
          <td class="p-2"><%= d.name %></td>
          <td class="p-2 text-right"><%= rp(d.amount_applied) %></td>
        </tr>
      <% }) %>
    </tbody>
    <tfoot>
      <tr class="border-t">
        <td></td><td class="p-2 font-semibold text-right">Total</td>
        <td class="p-2 text-right font-semibold"><%= rp(p.total_amount) %></td>
      </tr>
    </tfoot>
  </table>

  <% if (p.notes) { %>
    <div class="mt-3 text-sm"><span class="text-gray-500">Catatan:</span> <%= p.notes %></div>
  <% } %>

  <div class="mt-6 text-xs text-gray-500">Dicetak: <%= new Date().toLocaleString('id-ID') %></div>

  <div class="no-print mt-4 flex gap-2">
    <button onclick="window.print()" class="bg-gray-800 text-white px-3 py-2 rounded">Cetak</button>
    <a href="/admin/payments/new?santri_id=<%= p.santri_id %>" class="px-3 py-2 border rounded">Kembali</a>
  </div>
</div>
</body></html>
