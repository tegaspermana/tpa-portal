
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Masuk - Portal TPA</title>
  <link href="/public/css/output.css" rel="stylesheet" />
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="w-full max-w-sm bg-white p-6 rounded-xl shadow">
    <h1 class="text-xl font-semibold">Masuk</h1>
    <p class="text-sm text-gray-600 mb-4">Gunakan akun Anda</p>
    <% if (error) { %>
      <div class="mb-3 p-2 rounded text-sm bg-red-50 text-red-700"><%= error %></div>
    <% } %>
    <form method="post" action="/auth/login" class="space-y-3">
      <div>
        <label class="text-sm">Username / NIS</label>
        <input name="username" class="w-full border rounded px-3 py-2" required />
      </div>
      <div>
        <label class="text-sm">Kata Sandi</label>
        <input name="password" type="password" class="w-full border rounded px-3 py-2" required />
      </div>
      <button class="w-full bg-green-600 text-white py-2 rounded hover:bg-green-700">Masuk</button>
    </form>
  </div>
</body>
</html>
